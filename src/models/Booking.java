package src.models;

/**
 * Placeholder Booking model.
 * Currently reservations are represented by {@link FlightCustomerReservation}.
 * This class exists so the project compiles even if Booking is referenced.
 */
public class Booking {
    // Extend with fields/behaviour if/when needed.
}


